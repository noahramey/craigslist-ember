<div class="col-sm-10 col-sm-offset-1">
  {{#each category-list as |category|}}
    <div class="category-container">
      {{#link-to 'category' category.id}}<h3>{{ category.title }}</h3>{{/link-to}}
    </div>
  {{/each}}
</div>
